import React, { ReactElement } from 'react';

const SearchField = (): ReactElement => {
    return (
        <div>
            <input type="text"/>
        </div>
    );
};

export default SearchField;

